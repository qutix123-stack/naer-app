import React from "react";

import {
  View,
} from "react-native";

export default function SkeletonMessageCard() {
  return (
    <View
      style={{
        width: "100%",
        height: 120,
        marginBottom: 15,
        borderRadius: 22,
        backgroundColor: "#E5E7EB",
      }}
    />
  );
}